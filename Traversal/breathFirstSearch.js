// searching neighboring nodes before searching its children

var bfs = (tree) => {
  let node = tree;
  let children = node.children;
  while (node !== null) {
    if (children) {
      for (let i = 0; i < children.length; i++) {

      }
    }
  }
}